# Matriz de Riesgos

Probabilidad e impacto son juicios cualitativos (Alta/Media/Baja) basados
en la evidencia citada, no en medición estadística. Severidad =
combinación de ambos. "Responsable humano" es siempre el fundador en la
etapa actual (operador único, `Negocio_Velarix_v4.1.md` §8.2), salvo donde
se indique un rol distinto ya identificado (revisor financiero, abogado).

> **Actualizado 2026-07-23 (tras D-07)**: la columna "Fase" se reescribió
> fila por fila para reflejar la secuencia 1A–1E de
> `fases/FASE-01-EXACTITUD-FINANCIERA.md`. Se corrigió R-07 (no basta con
> autenticación — se requiere autorización privilegiada), se movió R-09
> a P0/1D, y se agregó R-18 (rollback incorrecto de 1E).

| ID | Dominio | Descripción | Evidencia | Probabilidad | Impacto | Severidad | Señal de detección | Mitigación | Fase | Responsable | Estado |
|---|---|---|---|---|---|---|---|---|---|---|---|
| R-01 | Financiero | Subcuentas no se suman (`.find()`), EBITDA/caja/opex mal calculados | `auditoria/04` #1 | Alta (ocurre en cualquier balance con >1 cuenta por rubro) | Crítico | Crítica | Prueba de especificación de 1A falla | Reescribir `getAccountValue`/`getVal`/`getValue` para sumar por `canonical_account`+período | **Fase 1B** | Fundador + revisor financiero | **Corregido en 1B-P0 (2026-07-23)** — `_shared/financial-accounts.ts`, ver `docs/velarix/bloque-1b/PRUEBAS-BL-02.md`. Pendiente de aprobación del revisor financiero externo |
| R-02 | Financiero | Conversión de moneda nunca se aplica (~4080x de error potencial) | `auditoria/04` (C) | Media (depende de que un cliente suba documento en moneda distinta a COP) | Crítico | Crítica | Prueba de especificación de 1A falla | Persistir `moneda_documento`/`factor_escala` desde `parse-document` hasta `build-structured-input` | **Fase 1B** | Fundador | **Corregido en 1B-P0 (2026-07-23)** — `_shared/currency.ts`, ver `docs/velarix/bloque-1b/PRUEBAS-BL-03.md`. Pendiente de aprobación del revisor financiero externo |
| R-03 | Financiero | PDF siempre muestra "USD" | `auditoria/04` #2 | Alta (ocurre en todo análisis en COP) | Alto | Crítica | Inspección visual de cualquier PDF en COP | Crear `formatMoneda(valor, moneda)` paramétrica y usarla en todo `pdf-generator.ts` | **Fase 1B** | Fundador | **Corregido en 1B-P0 (2026-07-23)** — `formatMoneda` en `pdf-generator.ts`, ver `docs/velarix/bloque-1b/PRUEBAS-BL-04.md` |
| R-04 | Financiero / Arquitectura | Split-brain: PDF (cliente) ignora resultado servidor + narrativa IA | `auditoria/04`, `auditoria/02`, `auditoria/08` | Alta (ocurre siempre, es el flujo por defecto) | Crítico | Crítica | Comparar `analyses.calculation_result` vs. resultado del PDF descargado | **Decidido (`D-06`)**: servidor es canónico. Diseño del contrato en 1A; activación real (conectar PDF/narrativa) solo en 1E | **Fase 1A (diseño) → Fase 1E (activación)** | Fundador | En progreso — decisión tomada, implementación pendiente |
| R-05 | Financiero | `ejecutar-calculo` lee campos inexistentes → `totalDebt` siempre 0 | `auditoria/04` (B) | Baja hoy (motor no se usa), Alta si se conecta | Crítico si se activa | Alta (ya no latente: motor declarado canónico por D-06) | Prueba de especificación de 1A (empresa apalancada) falla | Corregir el nombre del campo (`financial_debt_total`) | **Fase 1B** | Fundador + revisor financiero | **Corregido en 1B-P0 (2026-07-23)** — `_shared/capital-structure.ts`, ver `docs/velarix/bloque-1b/PRUEBAS-BL-05.md`. Pendiente de aprobación del revisor financiero externo |
| R-06 | Seguridad | `ejecutar-calculo` sin verificación de ownership | `auditoria/05` | Media (requiere que alguien conozca/enumere un `analysis_id` ajeno) | Crítico | Bloqueante | Prueba de invocación directa con `analysis_id` ajeno y clave anon | Agregar verificación `analysis.user_id === user.id`, igual que las demás funciones | **Fase 1D** | Fundador | **Corregido en 1D-P0 (2026-07-23)** — `_shared/authorization.ts` (`canExecuteCalculation`), ver `docs/velarix/bloque-1d/REPORTE-IMPLEMENTACION-1D-P0.md` |
| R-07 | Seguridad | `continuar-tras-revision` sin autenticación **ni autorización privilegiada** (corrección D-07: la verificación de ownership por sí sola es insuficiente, porque el propietario suele ser el cliente y el cliente no debe poder aprobar su propia revisión) | `auditoria/05` | Media | Crítico | Bloqueante | Prueba: cliente propietario intenta invocar la función para aprobar su propio análisis → debe fallar | Verificar que el llamante es analista autorizado, admin, o invocación interna legítima — no solo que el `analysis_id` le pertenece | **Fase 1D** | Fundador | **Corregido en 1D-P0 (2026-07-23)** — `_shared/authorization.ts` (`canContinueAfterReview`), ver `docs/velarix/bloque-1d/REPORTE-IMPLEMENTACION-1D-P0.md` |
| R-08 | Seguridad / Negocio | Sin control de rol real; auto-aprobación de revisión manual — **hallazgo nuevo en 1D-P0**: la autoaprobación era posible incluso con un UPDATE directo a `manual_reviews` (RLS `FOR ALL` permitía al propietario cambiar `estado`), no solo por falta de chequeo en la Edge Function | `auditoria/05`, `auditoria/07` | Alta (cualquier usuario puede probarlo hoy) | Alto (rompe el modelo de negocio, no solo seguridad técnica) | Bloqueante | Revisar si `profiles.role` se usa en alguna política/ruta | Recorte P0 (impedir autoaprobación) en 1D; sistema completo de asignación entre múltiples analistas en Fase 2 | **Recorte P0 en Fase 1D; sistema completo en Fase 2** | Fundador | **Recorte P0 corregido en 1D-P0** — Edge Function (`canContinueAfterReview`) y migración RLS de `manual_reviews` (creada, no aplicada). Sistema completo sigue abierto en Fase 2 |
| R-09 | Seguridad | `profiles` UPDATE sin `WITH CHECK`, riesgo de auto-escalar `role` | `auditoria/05` | Baja hoy (nada depende de `role` todavía) | Alto si se activa | Media, pero **P0** porque el control de rol de 1D no es válido mientras exista esta brecha | Intento de `UPDATE profiles SET role='admin'` desde un usuario normal | Agregar `WITH CHECK` que excluya la columna `role`, o mover `role` a una tabla separada gestionada solo por service role | **Fase 1D (`BL-10`, movido desde Fase 2, ahora P0)** | Fundador | **Migración creada y corregida en 1D-P0 (trigger + RPC `admin_set_user_role`), NO aplicada** — corrección de finalización (2026-07-23): se eliminó del trigger un registro de auditoría que no podía persistir (se revertía junto con el `RAISE EXCEPTION` que lo seguía en la misma transacción), y `admin_set_user_role` se rediseñó para devolver un resultado `jsonb` en vez de lanzar excepción, permitiendo auditar sus propios rechazos de forma durable. Rollback creado (`supabase/rollback/`). Aplicación bloqueada: sin Supabase CLI/psql/Docker ni credenciales de conexión en este entorno. La brecha sigue activa en cualquier entorno real hasta que un administrador ejecute la migración. Ver `docs/velarix/bloque-1d/REPORTE-IMPLEMENTACION-1D-P0.md` §12 |
| R-10 | Seguridad | `update-snapshots`/`enviar-notificacion` sin autenticación | `auditoria/05` | Media | Medio (integridad de datos compartidos / spam) | Media | Invocación directa sin JWT de usuario | Agregar autenticación mínima o mover a invocación solo por service role/cron | **Fase 2** | Fundador | Abierto |
| R-11 | Privacidad | Documentos completos y nombre de empresa enviados a Anthropic sin anonimizar | `auditoria/05` §5 | Alta (ocurre en cada análisis) | Medio (depende de política de datos de Anthropic, no confirmable desde el repo) | Media | Revisión manual de los prompts enviados | Evaluar minimización de datos antes de enviar (`Negocio_Velarix_v4.1.md` §12.3) | **Fase 2** | Fundador + abogado externo | Abierto |
| R-12 | Cumplimiento | Sin eliminación real de datos (solo soft-delete) | `auditoria/05` §8 | Alta | Alto (obligación legal potencial, Ley 1581) | Bloqueante | Solicitud de eliminación de un cliente real que no se puede cumplir | Implementar borrado real de documentos/derivados + `storage.remove()` | **Fase 2** | Fundador + abogado externo | Abierto |
| R-13 | Calidad | Cero cobertura de pruebas del motor financiero | `auditoria/04`, `auditoria/06` | Alta (certeza, es un hecho actual) | Crítico (cualquier cambio futuro puede reintroducir bugs sin detectarlo) | Crítica | — (es un estado, no un evento) | Crear casos dorados con datos conocidos + pruebas de regresión, aprobados por el revisor financiero externo | **Fase 1C** | Fundador + revisor financiero | Abierto |
| R-14 | Confiabilidad | `run-analysis-pipeline` no aborta ante falla de `map-accounts` | `auditoria/06` | Media (depende de que la IA falle en ese paso) | Alto (análisis "completado" con datos mal mapeados, sin aviso) | Alta | Log de `map-accounts` con error pero pipeline continuó | Agregar `return`/abort explícito en ese catch, igual que los otros 4 pasos | **Fase 1B** | Fundador | **Corregido en 1B-P0 (2026-07-23)** — `_shared/pipeline-guards.ts`, ver `docs/velarix/bloque-1b/PRUEBAS-BL-06.md`. Verificación de integración end-to-end pendiente (bloqueo de Deno) |
| R-15 | Confiabilidad | Race condition en `map-accounts` sin lock propio | `auditoria/06` | Baja (requiere doble invocación casi simultánea) | Medio (filas duplicadas en `account_homologations`) | Media | Filas duplicadas para el mismo `analysis_id`+`canonical_account`+período | Agregar lock propio o constraint de unicidad | **Fase 2** | Fundador | Abierto |
| R-16 | Negocio / Confianza | Copy de landing usa "Metodología auditada" sin auditoría externa real todavía | `auditoria/07` | Alta (visible hoy) | Medio (riesgo reputacional, no técnico) | Media | Revisión legal del copy | Ajustar lenguaje antes de cualquier lanzamiento comercial | **Fase 4** | Fundador | Abierto |
| R-17 | Rendimiento | Bundle 1.76MB sin code-splitting | `auditoria/09` | Alta (estado actual confirmado) | Bajo (afecta tiempo de carga, no exactitud) | Baja | Medición real de LCP/TTI (pendiente, ver plan de medición) | `React.lazy` en rutas pesadas | **Fase 7** | Fundador | Abierto — no prioritario |
| R-18 | Arquitectura / Negocio | Riesgo de que el rollback del Bloque 1E vuelva silenciosamente al motor cliente para producir una valoración profesional, entregando una cifra potencialmente incorrecta para mantener disponibilidad | `D-07` (corrección explícita de este riesgo) | Baja si se sigue el plan documentado, Alta si se implementa bajo presión de un incidente en producción | Crítico (entregaría a un cliente real una cifra que la propia auditoría identificó como potencialmente distinta) | Crítica | Revisar el plan de rollback de 1E antes de cualquier incidente real | El rollback permitido es: desactivar el flujo nuevo, bloquear la descarga profesional, restaurar la última versión servidor conocida — nunca reactivar el motor cliente como fuente de una valoración profesional | **Fase 1E** | Fundador | Mitigado por diseño (documentado en D-07 y en el plan de rollback de 1E) — pendiente de que la implementación real lo respete |
| R-19 | Financiero / Arquitectura | `ejecutar-calculo` hardcodea 7 supuestos (`taxRate`, `capexPct`, `wcPct`, `terminalGrowth`, `costOfDebt`, `riskFreeRate`, `erp`) que el motor cliente (`financial-engine.ts`) expone como campos ajustables de `FinancialInputs` — si el usuario cambia alguno en el formulario, el motor servidor (canónico por `D-06`) lo ignorará por completo al calcular | `ejecutar-calculo/index.ts:102-108` (constantes fijas) vs. `financial-engine.ts:112-120` (mismos campos, ajustables) — hallazgo nuevo de `BL-26`, ver `docs/velarix/bloque-1a/BL-26-tabla-comparativa-motores.md` sección 4 | Alta (ocurre en cualquier análisis donde el usuario ajuste un supuesto por defecto) | Alto (el resultado servidor ignoraría un supuesto que el usuario cree haber fijado) | Alta | Comparar el `input_payload` (con el supuesto ajustado) contra el `calculation_result` real y verificar si el supuesto se refleja | Decidir en Bloque 1B/1C si `ejecutar-calculo` debe leer estos 7 valores del `input` en vez de usar constantes fijas, o si el formulario del cliente debe dejar de exponerlos como ajustables mientras el servidor no los use | **Fase 1B/1C** (decisión pendiente, no resuelta en Bloque 1A) | Fundador + revisor financiero | Abierto — identificado en Bloque 1A (BL-26), no corregido |

## Cómo se usa esta matriz

Cada ítem se cierra únicamente cuando: (a) existe una corrección con
evidencia (commit/PR), (b) existe la prueba de regresión correspondiente
pasando, y (c) — para los ítems que lo requieran según
`Negocio_Velarix_v4.1.md` §9.4 — el revisor financiero o legal externo lo
aprobó. El estado se actualiza aquí, no se borra la fila (mantener
historial).
